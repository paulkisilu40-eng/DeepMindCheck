[phases.setup]
nixPkgs = ["python311", "sqlite"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate --noinput && gunicorn deepmindcheck.wsgi:application --bind 0.0.0.0:$PORT"